// TEMPORARY RECOVERY: the previous gzip/base64 payload in this chunk is corrupt at runtime.
// Keep the module importable so one bad chunk cannot prevent the whole game from starting.
export const TABU_2 = [];
console.warn('[Angu] TABU_2 deshabilitado temporalmente: payload comprimido corrupto. El resto del banco de Tabú sigue disponible.');
